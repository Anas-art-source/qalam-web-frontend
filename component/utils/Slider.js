import React, { memo } from "react";
import styles from "./Slider.module.css";

export default memo(function Slider() {
  const [slides, setSlides] = React.useState(["a", "b", "c"]);

  return <div></div>;
});
